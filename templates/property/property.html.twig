<div class="card mt-4">
    <div class="card body">
        <h5 class="card-title">
            <a href="{{ path('property.show',{slug: property.slug, id: property.id}) }}">
                {{ property.title }}
            </a>
        </h5>
        <p class="card-title">{{ property.description|nl2br }} ( {{ property.postaleCode }} )</p>
        <div class="text-primary" style="font-size: 2rem;font-weight:bold">
            {{ property.price }} €
        </div>
    </div>
</div>